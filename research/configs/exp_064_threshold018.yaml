# Pedido del usuario: marcha mas lenta. Subir threshold de feet_air_time (0.12->0.18) sobre el
# campeon con epochs=8 -- la vez anterior (exp_031, threshold=0.20, epochs=5) esto empeoro, pero
# nunca se probo bajo el regimen mas estable de epochs=8.
env:
  rewards:
    feet_air_time:
      params:
        threshold: 0.18
