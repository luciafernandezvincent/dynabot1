# Barrido del threshold ganador de exp_002 (0.12s) bajo delay=5: probar mas bajo (0.08s), aislado
# contra baseline igual que exp_002, para ver si hay margen de mejora o si 0.12 ya es cerca del
# optimo bajo delay.
env:
  rewards:
    feet_air_time:
      params:
        threshold: 0.08
