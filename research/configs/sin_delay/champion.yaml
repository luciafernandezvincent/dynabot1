# CAMPEON VIGENTE de la tanda sin_delay: exp_112_epochs_8 (score 0.5887).
# Camino: baseline 0.5173 -> exp_102 joint_deviation -> exp_103 +feet_air_time ->
# exp_104 +gait -> exp_107 +foot_impact -> exp_109/110 ajuste del trade-off 0.5802 ->
# exp_112 PPO num_learning_epochs 5->8: 0.5887 (mejoro 7 de los 8 terminos a la vez).
env:
  rewards:
    joint_deviation:
      weight: -0.2
    feet_air_time:
      weight: 2.25
    gait:
      weight: 5.0
    foot_impact:
      weight: -0.015
      params:
        threshold: 50.0
agent:
  algorithm:
    num_learning_epochs: 8
