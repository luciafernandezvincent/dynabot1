# EXCEPCION PUNTUAL (no es un delta de reward): habilita enabled_self_collisions en el codigo del
# robot (source/dynabot1/dynabot1/assets/dynabot.py), autorizada por el usuario el 21/08/2026 tras
# observar en video que la pata casi toca el muslo al caminar hacia atras.
#
# Motivo: con self-collision=False el simulador no detecta ese contacto, asi que undesired_contacts
# (ya cableado, peso -1.0 sobre .*arm_link) nunca podia penalizarlo por mas que existiera.
#
# REGIMEN NUEVO: a partir de aca la fisica de colision cambio. Los scores de esta corrida NO son
# directamente comparables contra baseline_ar/exp_001-012 (todos entrenados con self-collision
# apagado). Se corre sobre el campeon (foot_clearance w=0.5, target 5cm) para ver si arregla el
# problema sin romper el resto. Riesgo: puede aparecer contacto nuevo en gestos que antes pasaban
# gratis (base/hombro), mas caidas de las esperadas mientras la politica aprende a evitarlo.
#
# Sin cambios de reward: este YAML no tiene contenido, el cambio esta en el codigo del robot.
